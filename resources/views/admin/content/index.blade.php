@extends('layouts.admin') @section('heading',$title) @section('content')<div class="admin-actions"><a class="button primary" href="{{ route('admin.content.create',$type) }}">إضافة جديد</a></div><div class="admin-panel table-wrap"><table><thead><tr><th>#</th><th>العنوان</th><th>الحالة</th><th>آخر تعديل</th><th>الإجراءات</th></tr></thead><tbody>@forelse($items as $item) @php($label=$item->title_ar ?? $item->name_ar ?? $item->question_ar ?? $item->key ?? ('عنصر '.$item->id))<tr><td>{{ $item->id }}</td><td>{{ $label }}</td><td>{{ isset($item->status)?$item->status:((isset($item->is_published)&&!$item->is_published)||(isset($item->is_visible)&&!$item->is_visible)?'مخفي':'نشط') }}</td><td>{{ $item->updated_at?->format('Y-m-d H:i') }}</td><td class="row-actions"><a href="{{ route('admin.content.edit',[$type,$item->id]) }}">تعديل</a><form method="post" action="{{ route('admin.content.destroy',[$type,$item->id]) }}" onsubmit="return confirm('تأكيد حذف العنصر؟')">@csrf @method('delete')<button>حذف</button></form></td></tr>@empty<tr><td colspan="5">لا توجد عناصر.</td></tr>@endforelse</tbody></table></div>{{ $items->links() }}@endsection
