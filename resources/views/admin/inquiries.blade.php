@extends('layouts.admin') @section('heading','استفسارات التواصل') @section('content')<div class="admin-panel table-wrap"><table><thead><tr><th>المرسل</th><th>الموضوع</th><th>الرسالة</th><th>الحالة</th><th>التاريخ</th><th></th></tr></thead><tbody>@forelse($items as $item)<tr><td><strong>{{ $item->name }}</strong><br><a href="mailto:{{ $item->email }}">{{ $item->email }}</a></td><td>{{ $item->subject }}</td><td class="message-cell">{{ $item->message }}</td><td>{{ $item->status }}</td><td>{{ $item->created_at->format('Y-m-d H:i') }}</td><td>@if($item->status==='new')<form method="post" action="{{ route('admin.inquiries.close',$item->id) }}">@csrf @method('patch')<button class="text-link">إغلاق</button></form>@endif</td></tr>@empty<tr><td colspan="6">لا توجد استفسارات.</td></tr>@endforelse</tbody></table></div>{{ $items->links() }}@endsection
